// Autosap
alias +autosap "slot2; +attack"
alias -autosap "-attack; lastinv"
bind alt +autosap

// re-enable inspect
bind 2 +inspect

// instead of lastinv we do this so we always either get knife or revolver
bind q "slot3;slot1"

loadouts.spy